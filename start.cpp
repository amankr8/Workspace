#include <bits/stdc++.h>
using namespace std;

void handle_io() {
    #ifndef ONLINE_JUDGE
    freopen("input.txt", "r", stdin);
    #endif
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
}

void solve() {
}

int main() {
    handle_io();
    int t;
    cin >> t;
    while(t--) solve();
    return 0;
}
