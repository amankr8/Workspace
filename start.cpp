#include <bits/stdc++.h>
using namespace std;

void handle_io() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);
}

void solve() {
}

int main() {
    handle_io();
    int t;
    cin >> t;
    while(t--) solve();
    return 0;
}
