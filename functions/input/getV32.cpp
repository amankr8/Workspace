#include <bits/stdc++.h>
using namespace std;

// Input a vector<long long>
vector<int> getV32() {
    int n;
    cin >> n;
    vector<int> v(n);
    for(int i=0; i<v.size(); i++) cin >> v[i];
    return v;
}
